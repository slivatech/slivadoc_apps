import 'package:flutter/material.dart';

class SlivaShopPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SlivaShop'),
      ),
      body: const Center(
        child: Text('Halaman Belanja Kebutuhan hewan perliharaan'),
      ),
    );
  }
}