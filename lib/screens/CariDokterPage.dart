import 'package:flutter/material.dart';

class CariDokterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cari Dokter'),
      ),
      body: const Center(
        child: Text('Halaman Cari Dokter'),
      ),
    );
  }
}