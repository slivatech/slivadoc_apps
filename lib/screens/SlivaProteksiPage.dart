import 'package:flutter/material.dart';

class SlivaProteksiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sliva Proteksi'),
      ),
      body: const Center(
        child: Text('Halaman Sliva Proteksi'),
      ),
    );
  }
}