import 'package:flutter/material.dart';

class ArtikelKesehatanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Artikel'),
      ),
      body: const Center(
        child: Text('Halaman Artikel Kesehatan Hewan'),
      ),
    );
  }
}