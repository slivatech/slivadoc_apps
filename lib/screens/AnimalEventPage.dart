import 'package:flutter/material.dart';

class AnimalEventPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Animal Event'),
      ),
      body: const Center(
        child: Text('Halaman Event Animal Expo'),
      ),
    );
  }
}